ADMIN_ALLOWED_EMAIL_DOMAINS=csudh.edu,csi3.org
ADMIN_INVITE_CODE=replace-with-private-admin-invite-code

JWT_SECRET_KEY=replace-with-a-long-random-secret
JWT_ALGORITHM=HS256
JWT_EXPIRE_MINUTES=480

AUTH_COOKIE_NAME=fablab_admin_session
AUTH_COOKIE_SECURE=false

# Comma-separated list of additional frontend origins allowed to call the API
# (localhost:3000 and 127.0.0.1:3000 are always allowed). Use this for a
# Codespaces-forwarded URL or any other non-local demo origin, e.g.:
# CORS_ALLOWED_ORIGINS=https://your-codespace-3000.app.github.dev
CORS_ALLOWED_ORIGINS=